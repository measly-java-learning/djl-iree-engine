.gradle
**/build/
!**/src/**/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Avoid ignore Gradle wrappper properties
!gradle-wrapper.properties

# Cache of project
.gradletasknamecache

# Eclipse Gradle plugin generated files
# Eclipse Core
.project
# JDT-specific (Eclipse Java Development Tools)
.classpath

# Ignore Gradle build output directory
build

# Ignore Kotlin plugin data
.kotlin

# Native QA / bench outputs (container-produced). NOTE: native/bench/ holds
# bench SOURCE (iree_copy_bench.cpp, referenced by CMakeLists.txt) and must not
# be ignored — bench outputs go to native/bench-results/ and build/bench-models/.
native/bench-results/
native/asan/
native/qa_noasan/
native/qa/
native/tsan/

src/main/resources/native/**/*.so
src/main/resources/native/**/*.dylib
src/main/resources/native/**/*.dll
src/main/resources/native/**/*.json
src/main/resources/native/**/licenses/
*.rpm
bin/
third-party/
